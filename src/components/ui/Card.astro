---
import { cn } from '@/lib/utils'
import type { HTMLAttributes } from 'astro/types'

export interface Props extends HTMLAttributes<'div'> {
  variant?: 'default' | 'sci-fi' | 'glass' | 'elevated'
  class?: string
}

const { variant = 'default', class: className = '', ...props } = Astro.props

const cardVariants = {
  variant: {
    default: 'rounded-lg border bg-card text-card-foreground shadow-sm',
    'sci-fi':
      'group relative bg-cetus-surface border border-cetus-hairline border-neon backdrop-blur-sm rounded-2xl hover:border-cetus-accent/60 transition-all duration-300 transform hover:scale-105 hover-glow-cyan',
    glass:
      'bg-cetus-surface/80 rounded-3xl border border-cetus-hairline border-neon backdrop-blur',
    elevated:
      'rounded-lg border bg-card text-card-foreground shadow-lg hover:shadow-xl transition-shadow duration-300',
  },
}

const classes = cn(cardVariants.variant[variant], className)
---

<div class={classes} {...props}>
  <slot />
</div>
